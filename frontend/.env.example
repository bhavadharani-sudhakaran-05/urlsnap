# Direct connection to Express API (recommended for dev)
VITE_API_URL=http://localhost:5000/api

# Optional: Vite proxy target when using baseURL /api instead
# VITE_API_PROXY_TARGET=http://localhost:5000
